import 'package:flutter/material.dart';

class RFIDScannerSettingsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("RFID Scanner Settings")),
      body: Center(child: Text("Manage RFID Scanner Settings")),
    );
  }
}
